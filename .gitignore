.DS_Store
**/.vscode

# local-only reference material (private notes, vendor docs, pinout
# tables) — kept on disk for our own use, not meant to be pushed;
# reference/private/ also nests its own local-only .git repo
reference/

# ESP-IDF build artifacts
firmware/build/
firmware/sdkconfig.old
firmware/managed_components/